interface Presets {
    [key: string]: any;
}
export declare const presets: Presets;
interface GetPresetOptions {
    name?: string;
    random?: boolean;
    count?: number;
}
export declare function getPresets(options?: GetPresetOptions): Presets;
export {};
